name: Trivy Scan (On-Demand)

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Repo Tag and Docker image to scan"
        required: true
        default: "v1.0.0"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  scan:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read
      packages: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@65b78e6e13532edd9afa3aa52ac7964289d1a9c1
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Scan Dockerfile (config)
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: config
          scan-ref: ./src
          format: json
          output: vulnerability-config-results.json

      - name: Upload vulnerability config scan results
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-config-scan
          path: vulnerability-config-results.json

      - name: Scan built image (image)
        uses: aquasecurity/trivy-action@0.28.0
        with:
          scan-type: image
          image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.event.inputs.image_tag }}
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'
          format: json
          output: vulnerability-image-results.json

      - name: Upload vulnerability image scan results
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-image-scan
          path: vulnerability-image-results.json